import { IdentityMatcher, ObjectMatcher, StringMatcher } from './pattern-matching/index.js';
/**
 * @experimental
 *
 * @param value
 * @returns {PatternMatcher<any, any, any, any>}
 */
export function match(value) {
    switch (true) {
        case typeof value === 'string':
            return new StringMatcher(value);
        case typeof value === 'object':
            return new ObjectMatcher(value);
        default:
            return new IdentityMatcher(value);
    }
}
//# sourceMappingURL=match.js.map