import { MatchesAnything } from './rules/index.js';
/**
 * @access private
 */
export class PatternMatcher {
    value;
    rules;
    constructor(value, rules = []) {
        this.value = value;
        this.rules = rules;
    }
    else(transformation) {
        const matching_rule = this.rules.find(m => m.matches(this.value));
        return (matching_rule || new MatchesAnything(transformation)).execute(this.value);
    }
}
//# sourceMappingURL=PatternMatcher.js.map