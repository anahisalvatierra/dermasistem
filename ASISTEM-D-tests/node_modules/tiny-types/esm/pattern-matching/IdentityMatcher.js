import { PatternMatcher } from './PatternMatcher.js';
import { MatchesIdentical } from './rules/index.js';
/**
 * @access private
 */
export class IdentityMatcher extends PatternMatcher {
    when(pattern, transformation) {
        return new IdentityMatcher(this.value, this.rules.concat(new MatchesIdentical(pattern, transformation)));
    }
}
//# sourceMappingURL=IdentityMatcher.js.map