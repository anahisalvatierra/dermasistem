import { TinyType } from '../TinyType.js';
import { PatternMatcher } from './PatternMatcher.js';
import { MatchesEqualTinyType, MatchesIdentical, MatchesObjectsWithCommonPrototype } from './rules/index.js';
/**
 * @access private
 */
export class ObjectMatcher extends PatternMatcher {
    when(pattern, transformation) {
        return new ObjectMatcher(this.value, this.rules.concat(this.rule(pattern, transformation)));
    }
    rule(pattern, transformation) {
        switch (true) {
            case pattern instanceof TinyType:
                return new MatchesEqualTinyType(pattern, transformation);
            case typeof pattern === 'function':
                return new MatchesObjectsWithCommonPrototype(pattern, transformation);
            default:
                return new MatchesIdentical(pattern, transformation);
        }
    }
}
//# sourceMappingURL=ObjectMatcher.js.map