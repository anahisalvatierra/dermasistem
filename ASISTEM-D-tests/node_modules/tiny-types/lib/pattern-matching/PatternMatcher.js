"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.PatternMatcher = void 0;
const index_js_1 = require("./rules/index.js");
/**
 * @access private
 */
class PatternMatcher {
    value;
    rules;
    constructor(value, rules = []) {
        this.value = value;
        this.rules = rules;
    }
    else(transformation) {
        const matching_rule = this.rules.find(m => m.matches(this.value));
        return (matching_rule || new index_js_1.MatchesAnything(transformation)).execute(this.value);
    }
}
exports.PatternMatcher = PatternMatcher;
//# sourceMappingURL=PatternMatcher.js.map