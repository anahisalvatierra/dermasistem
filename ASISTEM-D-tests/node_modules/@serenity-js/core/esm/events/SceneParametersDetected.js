import { ensure, isDefined } from 'tiny-types';
import { CorrelationId, ScenarioDetails, ScenarioParameters } from '../model/index.js';
import { Timestamp } from '../screenplay/index.js';
import { DomainEvent } from './DomainEvent.js';
/**
 * @group Events
 */
export class SceneParametersDetected extends DomainEvent {
    sceneId;
    details;
    parameters;
    static fromJSON(o) {
        return new SceneParametersDetected(CorrelationId.fromJSON(o.sceneId), ScenarioDetails.fromJSON(o.details), ScenarioParameters.fromJSON(o.parameters), Timestamp.fromJSON(o.timestamp));
    }
    constructor(sceneId, details, parameters, timestamp) {
        super(timestamp);
        this.sceneId = sceneId;
        this.details = details;
        this.parameters = parameters;
        ensure('sceneId', sceneId, isDefined());
        ensure('details', details, isDefined());
        ensure('parameters', parameters, isDefined());
    }
}
//# sourceMappingURL=SceneParametersDetected.js.map