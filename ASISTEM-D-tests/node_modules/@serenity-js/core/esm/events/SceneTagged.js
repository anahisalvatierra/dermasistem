import { ensure, isDefined } from 'tiny-types';
import { CorrelationId, Tags } from '../model/index.js';
import { Timestamp } from '../screenplay/index.js';
import { DomainEvent } from './DomainEvent.js';
/**
 * @group Events
 */
export class SceneTagged extends DomainEvent {
    sceneId;
    tag;
    static fromJSON(o) {
        return new SceneTagged(CorrelationId.fromJSON(o.sceneId), Tags.fromJSON(o.tag), Timestamp.fromJSON(o.timestamp));
    }
    constructor(sceneId, tag, timestamp) {
        super(timestamp);
        this.sceneId = sceneId;
        this.tag = tag;
        ensure('sceneId', sceneId, isDefined());
        ensure('tag', tag, isDefined());
    }
}
//# sourceMappingURL=SceneTagged.js.map