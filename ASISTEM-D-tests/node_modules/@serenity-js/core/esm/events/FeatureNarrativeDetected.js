import { ensure, isDefined } from 'tiny-types';
import { CorrelationId, Description } from '../model/index.js';
import { Timestamp } from '../screenplay/index.js';
import { DomainEvent } from './DomainEvent.js';
/**
 * @group Events
 */
export class FeatureNarrativeDetected extends DomainEvent {
    sceneId;
    description;
    static fromJSON(o) {
        return new FeatureNarrativeDetected(CorrelationId.fromJSON(o.sceneId), Description.fromJSON(o.description), Timestamp.fromJSON(o.timestamp));
    }
    constructor(sceneId, description, timestamp) {
        super(timestamp);
        this.sceneId = sceneId;
        this.description = description;
        ensure('sceneId', sceneId, isDefined());
        ensure('description', description, isDefined());
    }
}
//# sourceMappingURL=FeatureNarrativeDetected.js.map