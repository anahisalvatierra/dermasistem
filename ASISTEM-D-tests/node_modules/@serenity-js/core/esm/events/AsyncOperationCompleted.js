import { ensure, isDefined } from 'tiny-types';
import { CorrelationId } from '../model/index.js';
import { Timestamp } from '../screenplay/index.js';
import { DomainEvent } from './DomainEvent.js';
/**
 * @group Events
 */
export class AsyncOperationCompleted extends DomainEvent {
    correlationId;
    static fromJSON(o) {
        return new AsyncOperationCompleted(CorrelationId.fromJSON(o.correlationId), Timestamp.fromJSON(o.timestamp));
    }
    constructor(correlationId, timestamp) {
        super(timestamp);
        this.correlationId = correlationId;
        ensure('correlationId', correlationId, isDefined());
    }
}
//# sourceMappingURL=AsyncOperationCompleted.js.map