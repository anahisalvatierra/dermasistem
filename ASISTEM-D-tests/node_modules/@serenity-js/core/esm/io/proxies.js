/**
 * @private
 */
export function caseInsensitive(dictionary) {
    return new Proxy(dictionary, {
        get: (obj, key) => {
            const found = Object.keys(obj)
                .find(k => k.toLocaleLowerCase() === key.toLocaleLowerCase());
            return found && obj[found];
        },
    });
}
//# sourceMappingURL=proxies.js.map