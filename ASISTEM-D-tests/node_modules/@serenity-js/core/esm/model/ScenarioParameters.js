import { ensure, isDefined, TinyType } from 'tiny-types';
import { Description } from './Description.js';
import { Name } from './Name.js';
export class ScenarioParameters extends TinyType {
    name;
    description;
    values;
    static fromJSON(o) {
        return new ScenarioParameters(Name.fromJSON(o.name), Description.fromJSON(o.description), o.values);
    }
    constructor(name, description, values) {
        super();
        this.name = name;
        this.description = description;
        this.values = values;
        ensure('name', name, isDefined());
        ensure('description', description, isDefined());
        ensure('values', values, isDefined());
    }
}
//# sourceMappingURL=ScenarioParameters.js.map